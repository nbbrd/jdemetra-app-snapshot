name: JDemetra+ v3 latest snapshot

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *' # run at 2 AM UTC

jobs:
  snapshot-job:
    if: startsWith(github.repository, 'nbbrd/') && startsWith(github.ref, 'refs/heads/main')
    strategy:
      matrix:
        java: [ 17 ]
        os: [ ubuntu-latest ]

    name: Snapshot on main
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout source code
        uses: actions/checkout@v3
        with:
          submodules: true
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: ${{ matrix.java }}
          cache: 'maven'

      - name: Build jdplus-main snapshots
        run: |
          git clone -b develop --single-branch --depth=1 https://github.com/jdemetra/jdplus-main.git jdplus-main
          mvn -B package -Pfast-build -DskipTests -T1C -f ./jdplus-main/pom.xml

      - name: Create asset with bundled Windows JRE
        run: |
          pushd ./jdplus-main/jdplus-main-desktop/jdplus-main-desktop-bin/target
          curl -sSL https://github.com/adoptium/temurin17-binaries/releases/download/jdk-17.0.6%2B10/OpenJDK17U-jre_x64_windows_hotspot_17.0.6_10.zip -o jre-windows.zip
          unzip jre-windows.zip -d nbdemetra
          echo "jdkhome=jdk-17.0.6+10-jre" >> nbdemetra/etc/nbdemetra.conf
          zip -r jdemetra-3.0.0-SNAPSHOT-bin-windows.zip nbdemetra
          popd

      - name: Setup Timestamp
        run: echo "timestamp=$(date +%Y%m%d%H%M%S)" >> $GITHUB_ENV

      - name: Add timestamp to assets names
        run: |
          mv ./jdplus-main/jdplus-main-desktop/jdplus-main-desktop-bin/target/jdemetra-3.0.0-SNAPSHOT-bin.zip "jdemetra-3.0.0-SNAPSHOT+${{env.timestamp}}-bin.zip"
          mv ./jdplus-main/jdplus-main-desktop/jdplus-main-desktop-bin/target/jdemetra-3.0.0-SNAPSHOT-bin-windows.zip "jdemetra-3.0.0-SNAPSHOT+${{env.timestamp}}-bin-windows.zip"

      - name: Create release and upload assets
        uses: ncipollo/release-action@v1
        with:
          name: ${{ github.workflow }}
          bodyFile: 'README.md'
          allowUpdates: true
          tag: 'latest'
          removeArtifacts: true
          artifacts: './jdemetra-*-bin*.zip'
          token: ${{ secrets.GITHUB_TOKEN }}
